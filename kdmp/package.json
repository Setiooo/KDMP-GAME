{
  "name": "kdmp",
  "version": "1.0.0",
  "description": "KDMP — Koperasi Desa Merah Putih: game simulasi ekonomi desa berbasis koperasi.",
  "license": "MIT",
  "scripts": {
    "copy:www": "node scripts/copy-www.js",
    "android:add": "npm run copy:www && npx cap add android",
    "android:sync": "npm run copy:www && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/android": "^6.1.0"
  }
}
